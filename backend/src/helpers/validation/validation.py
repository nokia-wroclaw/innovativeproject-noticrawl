from fastapi import HTTPException


# def validate_url(url: str) -> str:
#     # try:
#     #     return URL_VALIDATOR(url)
#     # except ValueError:
#     #     raise HTTPException(status_code=400, detail=url + " is not url")
